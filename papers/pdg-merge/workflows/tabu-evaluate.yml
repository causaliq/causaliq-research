
# Evaluate the graphs learnt by Tabu

steps:
  - name: "Evaluate graph learnt by Tabu"
    uses: "causaliq-analysis"
    with:
      action: evaluate_graph
      reference: networks/{{network}}/{{network}}.xdsl
      input: "papers/pdg-merge/results/tabu.db"
      metric: [f1, shd]
