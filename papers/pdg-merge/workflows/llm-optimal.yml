
# Derive an optimal graph from each LLM-generate PDG

steps:

 - name: "generate an optimal DAG"
   uses: "causaliq-analysis"
   with:
     action: "best_graph"
     threshold: 0.5
     input: papers/pdg-merge/results/llm.db
