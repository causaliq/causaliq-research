
# Evaluate each LLM optimal graph

steps:

 - name: "summarise the results"
   uses: "causaliq-analysis"
   with:
     action: "summarise"
     input: papers/pdg-merge/results/llm.db
     metric: [f1.mean, f1.count, shd.mean, shd.count]
     output: papers/pdg-merge/results/llm.csv